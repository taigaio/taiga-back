{% extends "emails/base-body-html.jinja" %}

{% block body %}
    {% trans signature=sr("signature"), user=user.get_full_name(), changer=changer.get_full_name(), project=project.name, ref=snapshot.ref, subject=snapshot.subject, url=resolve_front_url("task", project.slug, snapshot.ref) %}
    <h1>New task created on {{ project }}</h1>
    <p>Hello {{ user }},<br />{{ changer }} has created a new task:</p>
    <p><strong>#{{ ref }} {{ subject }}</strong></p>
    <a class="button" href="{{ url }}" title="See Task #{{ ref }} {{ subject }}">See task</a>
    <p><small>{{ signature }}</small></p>
    {% endtrans %}
{% endblock %}
